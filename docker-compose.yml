version: '3.8'

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: circle3space-sql
    environment:
      SA_PASSWORD: "P@ssw0rd123Circle"
      ACCEPT_EULA: "Y"
    ports:
      - "1544:1433"
    volumes:
      - sqlserverdata:/var/opt/mssql

  web:
    build: .
    container_name: circle3space-web
    ports:
      - "5055:5000"
    environment:
      - ConnectionStrings__umbracoDbDSN=Server=sqlserver,1433;Database=C3Space;User ID=SA;Password=P@ssw0rd123Circle;TrustServerCertificate=true;
      - ASPNETCORE_URLS=http://+:5000
    depends_on:
      - sqlserver

volumes:
  sqlserverdata:
